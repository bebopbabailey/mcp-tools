# MCP Tools (Architecture)

This is a collection repo. Each tool lives in its own folder with its own
`pyproject.toml` and scripts.

Tools are designed to be run as MCP stdio servers by an MCP client.

