# MCP Tools (Collection)

This repository is vendored as a git submodule and contains MCP stdio tools.
These tools are invoked by an MCP client (no network listener by default).

Start here:
- `web-fetch/` (implements `web.fetch` and `search.web` via LiteLLM `/v1/search`)
- `docs/INTEGRATIONS.md`

